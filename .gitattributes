# It looks like most file types are converted
# to .uasset when imported or created in Unreal
# This will be updated if new types need to be tracked

*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap filter=lfs diff=lfs merge=lfs -text
